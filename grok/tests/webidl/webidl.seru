///                           [sir        ]  [sf        ]
from webidl`something` import SomeInterface, someFunction

function DoSomething(something any) {
    ///         [si         ]
    (something.(SomeInterface))
}