class SomePromise {
	function Then(callback function<void>(bool)) awaitable<bool> {
		return this
	}

	function Catch(callback function<void>(error)) awaitable<bool> {
		return this
	}
}

function DoSomething() {
	var promise SomePromise = SomePromise.new()
	var someInt int = 2

	someInt <- promise
}
