class SomePromise {
	function<awaitable<bool>> Then(callback function<void>(bool)) {
		return this
	}

	function<awaitable<bool>> Catch(callback function<void>(error)) {
		return this	
	}
}

function<void> DoSomething() {
	var<SomePromise> promise = SomePromise.new()
	var<int> someInt = 2

	someInt <- promise
}