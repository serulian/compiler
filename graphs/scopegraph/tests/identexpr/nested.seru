class SomeClass {
	operator compare(first SomeClass, second SomeClass) {
		return 1
	}
}

function DoSomething() {
	var someIndex = SomeClass.new()

	if /* first */someIndex < someIndex && /* second */someIndex >= someIndex {}
}
