class SomeClass {
	function DoSomething() {
		var sc SomeClass = SomeClass.new()
		var isi ISomeInterface = SomeClass.new()

		var scn SomeClass?
		var isn ISomeInterface?

		/* cnullclass */(scn ?? sc)
		/* cnullinter */(scn ?? isi)

		/* inullclass */(isn ?? sc)
		/* inullinter */(isn ?? isi)
	}
}

interface ISomeInterface {
	function DoSomething()
}
