class SomeClass {
	function<void> DoSomething() {
		var<SomeClass> sc
		var<ISomeInterface> isi

		var<SomeClass?> scn
		var<ISomeInterface?> isn

		/* cnullclass */(scn ?? sc)
		/* cnullinter */(scn ?? isi)

		/* inullclass */(isn ?? sc)
		/* inullinter */(isn ?? isi)
	}
}

interface ISomeInterface {
	function<void> DoSomething()
}