class SomeClass {
	function String() string { return '' }
}

function tagged(strings []string, values []stringable) string {
	return ''
}

function anothertagged(strings []string, values []stringable) bool {
	return true
}

function DoSomething(first string, sc SomeClass) {
	/* templatestr */(tagged`${sc}: This is a cool
	${first} string`)

	/* nonstrtemplatestr */(anothertagged`${sc}: This is a cool
	${first} string`)
}
