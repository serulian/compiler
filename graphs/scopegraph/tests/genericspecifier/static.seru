class SomeClass<T> {
	var value T

	constructor SomeConstructor<Q>(value T, otherValue Q) {
		return SomeClass<T>{value: value}
	}
}

function DoSomething() {
	var cons = /* cons */(SomeClass<int>.SomeConstructor<bool>)
	/* sc */(cons(123, false))
}
