@•typealias('stream')
interface Stream<T> {}

@•typealias('function')
class Function<T> {}

@•typealias('int')
class Integer {
	operator Range(start int, end int) {
		// TODO: fix this once we can actually create a stream
		return 2
	}
}

@•typealias('bool')
class Boolean {}

@•typealias('float64')
class Float64 {}