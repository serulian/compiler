function DoSomething() {
	((b, c) => true)('hello', 'hi')
	(<- DoSomethingAsync(3)) == 3
	(<- p) + (<- q)
	someBool ?? (<- DoSomethingAsync())
	(&st).SomeProperty
	first := (&st)?.SomeProperty ?? false
	(function() {})()
	"foo" + "bar" if true else false
	"foo" if "foo" + "bar" == "foobar" else false
	'somestring ' + ('suffix ' if allow_suffix else '')
}
