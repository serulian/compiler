$module('string', function () {
  var $static = this;
  $static.DoSomething = function () {
    $t.fastbox('hello world', $g.________testlib.basictypes.String);
    $t.fastbox("hi world", $g.________testlib.basictypes.String);
    $t.fastbox('single quote with "quoted"', $g.________testlib.basictypes.String);
    $t.fastbox("double quote with 'quoted'", $g.________testlib.basictypes.String);
    $t.fastbox("escaped \" quote", $g.________testlib.basictypes.String);
    $t.fastbox('escaped \' quote', $g.________testlib.basictypes.String);
    return;
  };
});
