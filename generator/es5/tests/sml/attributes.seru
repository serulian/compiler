function SimpleFunction(props []{any}) bool {
	return props["data-foo"].(string) == "bar" && props["an-attr-here"].(bool)
}

function TEST() any {
	return <SimpleFunction data-foo="bar" an-attr-here={true} />
}
